from django.apps import AppConfig

# This class tells Django about this app and gives it a name.
# Django uses this to register the app when the project starts.

class uploaderConfig(AppConfig):
    # The name of the app. Django uses this internally.
    name = 'uploader'
